<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="#506878"
    app:layout_constraintCircleRadius="67dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Top row - title and recipient badge -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/notification_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Title"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="@color/im_sky_blue" />

            <TextView
                android:id="@+id/notification_recipient"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="ADMIN"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="@color/text"
                android:background="@drawable/notification_badge"
                android:paddingHorizontal="10dp"
                android:paddingVertical="3dp" />

        </LinearLayout>

        <!-- Scond Row - Type and Date -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="4dp">

            <TextView
                android:id="@+id/notification_type"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="General"
                android:textSize="12sp"
                android:textColor="@color/im_sky_blue" />

            <TextView
                android:id="@+id/notification_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="May 3, 2026"
                android:textSize="12sp"
                android:textColor="@color/im_sky_blue" />

        </LinearLayout>

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginVertical="10dp"
            android:background="@color/gray_blue" />

        <!-- Message -->
        <TextView
            android:id="@+id/notification_msg"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Message"
            android:textSize="14sp"
            android:lineSpacingMultiplier="1.5"
            android:textColor="@color/im_sky_blue" />

        <!-- Status (bottom-right) -->
        <TextView
            android:id="@+id/notification_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="10dp"
            android:text="PENDING"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="@color/text"
            android:background="@drawable/notification_badge"
            android:paddingHorizontal="10dp"
            android:paddingVertical="3dp" />

    </LinearLayout>

</androidx.cardview.widget.CardView>